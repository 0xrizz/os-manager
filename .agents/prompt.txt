You are an expert Linux Systems Engineer and Windows Infrastructure Architect. We are executing a zero-USB, zero-external-backup full migration from Windows to bare-metal Debian GNU/Linux (GNOME, ext4) on this Lenovo IdeaPad 3 (81WD) with a 512GB NVMe SSD.

This session is running in an elevated Administrator terminal. Your task is to perform deep administrative diagnostics, execute the pre-migration maintenance, export the current WSL environment, and verify the final migration plan.

First, inspect the handoff and finalized blueprint files:
- .agents/HANDOFF.md
- docs/LINUX_MIGRATION_BLUEPRINT.md

Execute the following administrative tasks using Windows and Linux tools:
1. Verify elevated privileges to ensure full administrative execution.
2. Query BitLocker status on all volumes using `manage-bde.exe -status` to confirm partitions C: and D: are fully decrypted and protection is disabled.
3. Disable Windows Fast Startup and Hibernation to prevent NTFS partition locking using `powercfg.exe /h off`.
4. Run a file system repair and bad-sector check on Drive D: using `chkdsk.exe D: /f /r` to resolve the detected "Full Repair Needed" status.
5. Export the current WSL development environment (dotfiles, .ssh, git configs, dev directory) to Drive D: by creating an archive at `/mnt/d/wsl_backup/wsl_home_backup.tar.gz`.
6. Inspect the current UEFI boot entries and BCD store using `bcdedit.exe /enum all` and PowerShell `Get-SecureBootUEFI` to verify NVRAM entry registration capability.
7. Confirm the exact partition boundaries, sector offsets, and free space of Disk 0 using `powershell.exe -NoProfile -Command "Get-Partition -DiskNumber 0 | Format-Table -AutoSize"`.

Strict guardrails:
- Do not run any format or deletion commands on Partition 4 (Drive D: / DATA_STORE). Data on Drive D: (~201 GB) must remain completely untouched.
- Do not execute irreversible disk operations without explaining the exact parameters first.

Output deliverables:
- Summarize the status of BitLocker, Fast Startup, Drive D file system integrity, and WSL backup creation.
- Confirm the staging steps for DiskGenius (shrinking Drive C by 120 GB, creating 8 GB FAT32 DEBIAN_SET installer partition, and registering \EFI\BOOT\BOOTX64.EFI into UEFI NVRAM).
- Update docs/LINUX_MIGRATION_BLUEPRINT.md with the latest verified administrative data.

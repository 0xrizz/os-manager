# System Clean Skill

Perform safe cleanup of APT package caches, unused system dependencies, UV tool caches, and temporary build caches without altering system configs.

## Command
```bash
/home/rizz/dev/os-manager/scripts/clean_system.sh
```

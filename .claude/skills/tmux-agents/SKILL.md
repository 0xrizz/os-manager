# Multi-Agent Tmux Session Skill

Launches or reattaches to a paired multi-agent workspace running `agy` (Google Antigravity CLI), `claude` (Claude Code CLI), and a live monitoring pane.

## Command
```bash
/home/rizz/dev/os-manager/scripts/tmux_agents.sh
```
